package Collections;

import java.util.Collection;

public class BasicFuntions {

  public static void main(String[] args) {
    
    
    /*
    Collections.sort() : it sorts the lists
    Collections.reverse()
    Collections.shuffle()
    Collections.addAll() : it adds multiple entries at once to the list
    Collections.copy(target,source): it copies source to the target list (sizes should be equal to each other)
    Collections.fill(list,target) : it replaces all the existing entries with the target value, list's size should >= 1 
    
    
    
    
    
    */
  }
}

